import React from "react";

const BookPage = ({ book }) => {
  // Your component logic here
  return (
    <div>
      <h1>{book.title}</h1>
      {/* Other components and content */}
    </div>
  );
};

export default BookPage;
